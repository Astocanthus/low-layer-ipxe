# Environment variables - contains sensitive credentials
.env

# Authentication directory - contains generated .htpasswd files
auth/
# Keep the README to explain the auth directory purpose
!auth/README.md

# Images directory - contains large system image files
images/
# Keep the README to explain where to place Fedora CoreOS images
!images/fcos/README.md